# Momo local maintenance — 2.4.0-local

- Status: Codex Desktop follow-start implemented, installed and enabled by default. Source branch remains codex/daily-remaining-allowance; prior daily allowance and automatic care features included in this source update.
- Entry: right-click pet -> 跟随 Codex Desktop 启动. Saved FollowCodexDesktop defaults true, explicit false persists. Separate from existing Windows direct-pet autostart.
- Mechanism: dedicated HKCU Run value MomoCodexDesktopFollower starts Momo.exe --watch-codex at user login. Hidden listener polls every 2 seconds, identifies OpenAI.Codex WindowsApps ChatGPT.exe roots in the current session, filters CLI and Electron children, and launches --codex-follow-start only once per desktop instance when no pet exists.
- Lifecycle: manual pet exit is respected during the same desktop session. Desktop restart permits another launch. Desktop exit does not stop the pet. Disabling removes dedicated login registration and listener exits. Named mutexes prevent duplicate pet/listener processes. --stop-codex-watcher supports future executable replacement; wait for listener exit before replacing files.
- Owners: Services/CodexFollower.cs, App.xaml.cs, Services/PetSettings.cs, PetWindow.CodexFollower.cs, PetWindow.xaml.cs; tests in SelfTests.CodexFollower.cs and existing UI capture.
- Validation: Release build succeeded, 43 self/live checks and 50 related UI checks passed. Installed listener actually launched the pet from the running Codex desktop; manual exit stayed closed, removing registration stopped the listener, re-enabling relaunched the pet, duplicate listener exited. Installed binary hash and saved default true verified. Evidence and backup paths: artifacts/installed-codex-follower.json; docs/validation-codex-follower.md.
- Live smoke initially tried to close a process before its WPF window was ready. Rechecked after readiness; all behaviors passed, no app-code workaround required. Codex Desktop and its active tasks were not restarted or interrupted.
- Limits: login registration inspected but no Windows logout/login performed. Detection matches the currently installed Microsoft Store package, including version changes. Existing unrelated full geometry failure MOVE/climb.right remains documented; no animation changes in this update.
- Existing daily remaining/overdraw and auto-care features are retained. Pet saves and old executable were backed up. Installation and shortcuts stay at %LOCALAPPDATA%/Programs/MomoCodexPet.
- Next: ordinary use; disable follow-start via right-click if desired. DEBUG_HANDOFF.md unchanged because no new unresolved application defect was introduced.
