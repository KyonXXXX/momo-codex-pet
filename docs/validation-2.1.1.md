# 2.1.1 今日百分比显示与累计修复

- 气泡直接显示已记录的具体百分比，不再加 ≥ / ≤；最多保留两位小数，小用量不再被一位小数过早取整。
- 首次启用或缺失日历史的记录范围移至悬停说明，不伪造启用前的用量。
- 修复接口重置时间约 1 秒的抖动被误判为新周、反复清空今日累计的问题；同一窗口允许 120 秒时间差，真正的周窗口变化、日期变化、套餐变化和读数下降仍重新记录。
- 回归验证包括初次记录的数字显示、零值、小数、超额、重启后时间抖动、真实新窗口和原有每日计算测试。
